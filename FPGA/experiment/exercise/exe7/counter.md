## シミュレーションで確認したいこと

1. **リセット動作**
   * `rst` を有効にすると、`count` が `0000` に戻る
2. **クロックに同期してカウント**
   * 立ち上がり（または立下り）エッジごとに `count` が +1
   * `0000 → 0001 → 0010 → … → 1111 → 0000` と繰り返す
3. **enable がある場合**
   * `enable=0` のときは値が保持され、`enable=1` のときだけカウント進行
